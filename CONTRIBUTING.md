# Contributing (handover repo)
- All changes must pass pre-commit, ruff, black, mypy, pytest (85%+).
- No secrets in diffs. Use .env.example only.
- Commit messages: concise, imperative. Example: "auth: add JWT decode error detail".
